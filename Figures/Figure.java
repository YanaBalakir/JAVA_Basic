package Figures;

public interface Figure {
    // TODO: DK: В интерфейсе все методы по умолчанию public, т.е. слово "public" можно не писать (обычно так и делают). Оно никак не мешает и рабоать с ним будет правильно просто оно лишнее вданном контексте (IDE Тоже подсказывает убрать)
    public double area();
}
