public class Circle {
    private double radius = 1.7; // радиус
    private double area;//площадь
    final String type = "Circle";
    public Circle (double r){

    }
    public double countCircleS (double radius){
        this.radius = radius;
        return area = Math.PI * (radius*radius);

System.out.println();
    }
}
